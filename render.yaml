services:
  - type: web
    name: dialycore-api
    runtime: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeployTrigger: checksPass
    envVars:
      - key: APP_ENV
        value: production
      - key: PUBLIC_DEMO_MODE
        value: "true"
      - key: ENABLE_DEMO_GUEST
        value: "true"
      - key: DATABASE_URL
        value: sqlite:////app/data/dialycore_demo.db
      - key: CORS_ORIGINS
        value: ""
      - key: CORS_ORIGIN_REGEX
        value: '^https://dialycore(?:-[a-z0-9-]+)*\.vercel\.app$'
      - key: JWT_SECRET
        generateValue: true
      - key: DEMO_PATIENT_COUNT
        value: "300"
      - key: DEMO_SEED
        value: "2026"
